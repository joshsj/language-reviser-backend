{
  "name": "language-reviser-backend",
  "version": "1.0.0",
  "dependencies": {
    "dotenv": "^10.0.0",
    "picocolors": "^1.0.0",
    "typescript": "^4.4.4",
    "ws": "^8.2.3"
  },
  "devDependencies": {
    "@types/ws": "^5.1.0"
  },
  "scripts": {
    "prestart": "npx tsc --build",
    "start": "node -r dotenv/config build/index.js",
    "poststart": "npm run clean",
    "clean": "npx tsc --build --clean"
  }
}
