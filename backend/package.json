{
  "name": "language-revisor-backend",
  "version": "1.0.0",
  "dependencies": {
    "dotenv": "^10.0.0",
    "module-alias": "^2.2.2",
    "picocolors": "^1.0.0",
    "typescript": "^4.4.4",
    "ws": "^8.2.3"
  },
  "devDependencies": {
    "@types/ws": "^5.1.0"
  },
  "scripts": {
    "prestart": "npx tsc --build",
    "start": "node -r dotenv/config -r module-alias/register build/backend/src/index.js",
    "poststart": "npm run clean",
    "clean": "npx tsc --build --clean"
  },
  "_moduleAliases": {
    "@shared": "../shared"
  }
}
